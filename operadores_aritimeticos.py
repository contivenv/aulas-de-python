x = y = z = 0

# utilizaremos uma método chamado "casting" que faz a conversão de dados para nós para que o valor seja exibido
# quando colocamos o mouse em cima do da variável "x", percebemos que ele já converteu o tipo dela para inteiro
x = int(input('Digite um número:'))
y = int(input('Digite um outro número:'))

z = x + y

print('A soma dos dois valores é', z)